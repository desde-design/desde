## What changed

<!-- Describe the change. -->

## Why

<!-- What problem does this solve, or what does it improve? -->

## Gates run

<!-- Check the ones you ran. See CONTRIBUTING.md for what each one covers. -->

- [ ] `npm run verify`
- [ ] `cd editor-cli && npm run typecheck` (touched `editor-cli/`)
- [ ] `cd viewer && npx vitest run && npx tsc --noEmit -p tsconfig.json` (touched `viewer/`)
- [ ] `npm run build:bridge`, `BRIDGE_VERSION` bumped, and the rebuilt bundle committed (touched `src/bridge/`)
- [ ] `cd viewer && npm run build` or `cd website && npm run build` (changed a route or layout in one of those)
- [ ] First pull request to this repo? A bot will ask you to sign the Contributor License Agreement (`CLA.md`) on this PR.

## Anything reviewers should know

<!-- Trade-offs, things left out on purpose, open questions. -->
