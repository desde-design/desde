{
  "name": "@desde/editor-cli",
  "version": "0.1.0-d0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "Editor CLI core (D-0 milestone). Boots a Vite supervisor against the user's repo, serves the editor UI bundle, dispatches edits to source files.",
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "bin": {
    "desde": "./bin/desde.mjs",
    "desde-mcp": "./bin/desde-mcp.mjs"
  },
  "main": "./src/core.ts",
  "scripts": {
    "build": "npm run build:server && npm run build:ui",
    "build:server": "node scripts/build-server.mjs",
    "build:ui": "vite build --config ui-src/vite.config.ts",
    "dev": "tsx src/cli.ts",
    "start": "node bin/desde.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:smoke": "vitest run --config vitest.smoke.config.ts"
  },
  "dependencies": {
    "@babel/parser": "^7.29.3",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@vue/compiler-sfc": "^3.5.42",
    "playwright": "^1.62.1",
    "tailwind-merge": "^3.5.0",
    "vite": "^8.2.2",
    "vite-plugin-vue-tracer": "^1.5.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@vitejs/plugin-react": "^5.1.4",
    "esbuild": "^0.28.2",
    "tailwindcss": "^4",
    "tsx": "^4.23.13",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
